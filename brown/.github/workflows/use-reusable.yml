name: Use Reusable Workflow

on: push

jobs:
  # TODO: Create a job called 'call-setup' that:
  #   - calls ./.github/workflows/reusable-setup.yml
  #   - passes node-version: '18'
  # Remember: a job that uses a reusable workflow cannot have steps:
  ???:
    uses: ???
    with:
      ???: ???
